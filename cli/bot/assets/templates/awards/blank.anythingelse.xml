<template>
  <embed color="#faa61a">
    <title>Запрос редактируется</title>
      {{ .Author.Mention }}, **введите получателей для другой суммы *(если нужно)***.
      Отправте команду `-отправить`, если запрос готов.
      Отправте команду `-выдать`, если хотите указать ещё получателей.
    <fields>
      <field name="Причина">{{ .Reason }}</field>
        {{ range .Rewards }}
          <field name="Получат {{ .Amount }}">
            {{ range .Users }} {{ .Mention }} {{ end }}
          </field>
        {{ end }}
      <field name="Команды">
        ```md
        -отправить          отправляет запрос на рассмотрение
        -выдать  &lt;линки&gt;    указать получателей
        -сумма   &lt;сумма&gt;    изменить предыдущую сумму
        -причина &lt;причина&gt;  изменить причину
        -отменить           отменяет запрос
        ```
      </field>
    </fields>
    <footer>
      Время на ответ истечёт в {{ .ExpiresAt.Format "01.02.2006 15:04:05 MST" }}
    </footer>
  </embed>
</template>
