<template>
    <embed color="#faa61a">
        <title>Заявка</title>
            {{.Author.Mention}}, **Перечислите получателей**
            Отправте сообщение с упоминаниями получателей           
        <fields>            
            <field name="Причина">{{.Reason}}</field>
            {{ range .Rewards }}
            <field name="Получат {{ .Amount }}">
                {{range .Users}} {{ .Mention }} {{end}}
            </field>
            {{ end }}
            <field name="Команды">
                `-заявка &lt;причина&gt;` **создаёд заявку**
                `-сумма  &lt;сумма&gt;`   **указать сумму**
                `-выдать &lt;линки&gt;`   **линки получателей**
                `-отправить`              **отправляет заявку на рассмотрение**
                `-отменить`               **удаляет заявку**
            </field>
        </fields>	    
        <footer>
            Заявка истечёт в {{.ExpiresAt.Format "01.02.2006 15:04:05 MST"}}
        </footer>
    </embed>
</template>
