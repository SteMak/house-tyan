<template>
    <embed color="#ffa550">
        <title>{{ .Club.Symbol }}: {{ .Club.Title }}</title>
        {{ if .Club.Description }} {{ .Club.Description }} {{ end }}
        [EOL]
        {{ range .Members }}
        <@{{ .UserID }}>: {{ .XP }} xp
        {{ end }}
    </embed>
</template>