<template>
  <embed color="#faa61a">
    <title>Запрос редактируется</title>
      {{ .Author.Mention }}, **введите сумму**.
      Отправте команду `-сумма` с её указанием.
    <fields>
      <field name="Причина">{{ .Reason }}</field>
        {{ range .Rewards }}
          <field name="Получат {{ .Amount }}">
            <!-- if amount 0 = magic -->
            {{ range .Users }} {{ .Mention }} {{ end }}
          </field>
        {{ end }}
      <field name="Команды">
        ```md
        -сумма   &lt;сумма&gt;    указать сумму
        -выдать  &lt;линки&gt;    изменить предыдущих получателей
        -причина &lt;причина&gt;  изменить причину
        -отменить           отменяет запрос
        ```
      </field>
    </fields>
    <footer>
      Время на ответ истечёт в {{ .ExpiresAt.Format "01.02.2006 15:04:05 MST" }}
    </footer>
  </embed>
</template>
